Structure:

- The file combine_all_tracks.py is the one that extracts the samples from the midi files
- 'Classical Piano Midis.zip' is the dataset. We are not using all the composers in there

Requirements:
- pip install pretty_midi

Optional, you can comment this code out:
- pip install librosa

References:
- https://github.com/craffel/pretty-midi